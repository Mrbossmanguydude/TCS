"""
Utility package for non-UI project services.

Primary modules:
- `run_init.py`: deterministic seed setup and runtime persistence.
- `map_generation.py`: deterministic phase map generation for setup preview.
- `controller_prep.py`: VN + PN preparation helpers.
"""
